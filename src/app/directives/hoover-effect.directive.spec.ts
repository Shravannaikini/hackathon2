import { HooverEffectDirective } from './hoover-effect.directive';

describe('HooverEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new HooverEffectDirective();
    expect(directive).toBeTruthy();
  });
});
